package com.tallerwebi.presentacion;

import org.springframework.stereotype.Controller;

@Controller
public class UsuarioController {

    /*
    @Autowired
    private UsuarioRepository usuarioRepository;


    @Autowired
    private ClubRepository clubRepository;

    public void agregarUsuarioAClub(int usuarioId, int clubId) {
        Usuario usuario = usuarioRepository.findById(usuarioId).orElseThrow(() -> new RuntimeException("Usuario no encontrado"));
        Club club = clubRepository.findById(clubId).orElseThrow(() -> new RuntimeException("Club no encontrado"));

        usuario.getClubes().add(club);  // Agregar el club al usuario
        usuarioRepository.save(usuario);  // Guardar los cambios
    }
    */


}
